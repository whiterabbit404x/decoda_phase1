export default function SecurityPage() {
  return <main className="container marketingPage"><section className="dataCard"><h1>Security & Trust</h1><p>Decoda RWA Guard applies authentication controls, workspace-scoped authorization, audit logging, and secure secret handling patterns. Customers should configure verified email, strong passwords, and least-privilege workspace roles.</p></section></main>;
}
